import hashlib

def md5():
        a="abin"
        b=a.encode()
        c=hashlib.md5(b)
        hexa=c.hexdigest()
        print (hexa)
        var hashlib = sha512(salt + password)
for (i = 0; i < 1000; i++) {
    hashlib = sha512(hashlib);
}
        return

md5()